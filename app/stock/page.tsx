"use client";
import { FormEvent,useCallback,useEffect,useMemo,useState } from "react";import { WorkspaceShell } from "@/app/components/workspace-shell";import { api,Product } from "@/app/lib/api";import ui from "@/app/components/workspace-ui.module.css";
export default function StockPage(){const [products,setProducts]=useState<Product[]>([]),[search,setSearch]=useState(""),[barcode,setBarcode]=useState(""),[quantity,setQuantity]=useState("1"),[note,setNote]=useState(""),[loading,setLoading]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState("");const load=useCallback(async()=>{try{setProducts(await api<Product[]>("/products"))}catch(e){setError(e instanceof Error?e.message:"Could not load products.")}},[]);useEffect(()=>{const t=setTimeout(()=>void load(),0);return()=>clearTimeout(t)},[load]);const selected=products.find(p=>p.barcode===barcode);const matches=useMemo(()=>{const q=search.toLowerCase().trim();return q?products.filter(p=>[p.name,p.barcode,p.sku].some(v=>String(v??"").toLowerCase().includes(q))).slice(0,8):[]},[products,search]);async function submit(e:FormEvent){e.preventDefault();if(!barcode||Number(quantity)<1){setError("Choose a product and enter a quantity of at least 1.");return}setLoading(true);setError("");try{const updated=await api<Product>("/stock/add",{method:"POST",body:JSON.stringify({barcode,quantity,note})});setNotice(`${updated.name} stock updated from ${selected?.stock??0} to ${updated.stock}.`);setQuantity("1");setNote("");setSearch("");await load()}catch(e){setError(e instanceof Error?e.message:"Could not add stock.")}finally{setLoading(false)}}
 return <WorkspaceShell><div className={ui.head}><div><label>Inventory</label><h1>Add stock</h1><p>Find a product manually and add received inventory—no scanner required.</p></div></div>{error&&<div className={ui.error}>{error}</div>}{notice&&<div className={ui.notice}>{notice}</div>}<div style={{display:"grid",gridTemplateColumns:"minmax(0,1.2fr) minmax(280px,.8fr)",gap:18}}><section className={ui.panel}><h2 style={{marginTop:0,fontSize:17}}>Choose product</h2><div className={ui.field}><label>Search by name, barcode, or SKU</label><input className={ui.input} value={search} onChange={e=>setSearch(e.target.value)} placeholder="Start typing…"/></div>{matches.length>0&&<div style={{marginTop:10,border:"1px solid #e2e8f0",borderRadius:9,overflow:"hidden"}}>{matches.map(p=><button type="button" key={p.id} onClick={()=>{setBarcode(p.barcode);setSearch(p.name)}} style={{display:"flex",width:"100%",justifyContent:"space-between",padding:12,border:0,borderBottom:"1px solid #e2e8f0",background:p.barcode===barcode?"#ccfbf1":"white",cursor:"pointer",textAlign:"left"}}><span><strong>{p.name}</strong><small style={{display:"block",color:"#64748b",marginTop:3}}>{p.barcode}{p.sku?` · ${p.sku}`:""}</small></span><strong>{p.stock} in stock</strong></button>)}</div>}<div className={ui.field} style={{marginTop:15}}><label>Or enter barcode directly</label><div style={{display:"flex",gap:8}}><input className={ui.input} value={barcode} onChange={e=>setBarcode(e.target.value)} placeholder="Product barcode"/><button className={ui.secondary} type="button" onClick={()=>setSearch(products.find(p=>p.barcode===barcode)?.name??barcode)}>Find</button></div></div></section><form className={ui.panel} onSubmit={submit}><h2 style={{marginTop:0,fontSize:17}}>Stock details</h2>{selected?<div className={ui.notice}><strong>{selected.name}</strong><br/>Current stock: {selected.stock}</div>:<p className={ui.muted}>Select a product to continue.</p>}<div className={ui.field}><label>Quantity to add</label><input className={ui.input} type="number" min="1" step="1" value={quantity} onChange={e=>setQuantity(e.target.value)}/></div><div className={ui.field} style={{marginTop:14}}><label>Note (optional)</label><input className={ui.input} value={note} onChange={e=>setNote(e.target.value)} placeholder="Supplier, invoice, or reason"/></div><button className={ui.primary} style={{width:"100%",marginTop:20,height:48}} disabled={loading||!selected}>{loading?"Saving…":"Add stock"}</button></form></div></WorkspaceShell>}
