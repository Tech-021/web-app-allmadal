"use client";
import { useAuth } from "@/hooks/useAuth";
import { useRouter } from "next/navigation";
import { useEffect } from "react";
export default function DashboardPage() { const { user, isLoading, logout } = useAuth(); const router = useRouter(); useEffect(() => { if (!isLoading && !user) router.replace("/login"); }, [isLoading, user, router]); if (isLoading || !user) return <main className="grid min-h-screen place-items-center text-[#607067]">Loading workspace…</main>; return <main className="min-h-screen bg-[#f6f7f5] p-6 sm:p-10"><nav className="mx-auto flex max-w-6xl items-center justify-between"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#1d4934] font-bold text-white">AM</span><span className="font-semibold">Al Madel</span></div><button onClick={async () => { await logout(); router.push("/login"); }} className="rounded-xl border border-[#d8dfda] bg-white px-4 py-2 text-sm font-semibold">Sign out</button></nav><section className="mx-auto mt-24 max-w-3xl rounded-3xl border border-[#e0e5e1] bg-white p-8 shadow-sm sm:p-12"><span className="rounded-full bg-[#e9f3ed] px-3 py-1 text-xs font-bold uppercase tracking-wider text-[#1d4934]">{user.role}</span><h1 className="mt-5 text-4xl font-semibold tracking-tight">Welcome, {user.name}</h1><p className="mt-3 text-[#6c7871]">You are signed in to the Al Madel {user.role} workspace.</p></section></main>; }
